import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { ControlContainer } from "./index";

<Meta
  title="MDX/Box"
  component={Box}
/>

#ControlContainer

With `MDX`, we can define a story for `ControlContainer` right in the middle of our Markdown documentation.

<Canvas columns={2} withSource="open" withToolbar>
  <Story id="layout-box--red"
  args={{ 
      bgColor: 'red',
    }}>
  >
    <ControlContainer
      centerContent
      w="500px"
      h="100px"
      bgColor="var(--primary-200)"
      color="white"
      fSize="14px"
      p="4px"
    >
      This is the ControlContainer
    </ControlContainer>
  </Story>
</Canvas>
