import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { Box } from "./index";

<Meta
  title="MDX/Box"
  component={Box}
/>

#Box

With `MDX`, we can define a story for `Box` right in the middle of our Markdown documentation.

<Canvas columns={2} withSource="open" withToolbar>
  <Story id="layout-box--red"
  args={{ 
      bgColor: 'red',
    }}>
  >
    <Box
      centerContent
      w="500px"
      h="100px"
      bgColor="var(--primary-200)"
      color="white"
      fSize="14px"
      p="4px"
    >
      This is the Box
    </Box>
  </Story>
</Canvas>
